

//Vector
/*
 
 * find(v.begin(),v.end(),val) -> returns iterator
 * Custom vector sort 
    -> sort(v.begin(),v.end(),[](int a,int b){
            return a < b;
                });
 
 
*/

//Multimap, Map, unordered_map

// Multimap can be replaced by 2D vector with sort for cases where
// elements with equal key value should be sorted.
/*
 
 * insert
    -> m.insert({key,value});
 * erase
    -> m.erase(iterator);
 * find
    -> m.find(key)
 
 * 
 */



